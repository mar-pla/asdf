import React from "react";

export default function Technologies({ t }) {
  return (
    <section className="text-center p-8">
      <h1 className="text-2xl font-bold">{t.technologies}</h1>
    </section>
  );
}
